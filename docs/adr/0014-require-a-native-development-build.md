# Require a Native Development Build

This package requires a native development build or prebuilt app and does not support Expo Go. The module depends on native BLE permissions and ACS native SDK binaries that must be compiled into the Android and iOS apps.

