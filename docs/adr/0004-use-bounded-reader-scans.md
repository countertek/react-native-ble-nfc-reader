# Use Bounded Reader Scans

The v0.1 API exposes bounded Reader scans with a caller-provided timeout and emits discovery events during that scan window. It does not support indefinite background scanning until a real app workflow needs it.

