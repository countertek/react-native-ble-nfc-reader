# Do Not Support Web

The package does not support web. Any web entrypoint exists only to fail with a clear unsupported-platform error because the Reader workflow depends on native mobile BLE and ACS native SDKs.

