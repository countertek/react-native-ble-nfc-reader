# Expose Reader Metadata As Optional

The v0.1 API may expose Reader metadata such as battery level, firmware version, serial number, and model when ACS provides it. Metadata is optional and must not be required for scan or connect success because support can vary by Reader model and platform.

