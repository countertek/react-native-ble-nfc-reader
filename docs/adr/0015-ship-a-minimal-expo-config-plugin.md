# Ship A Minimal Expo Config Plugin

The package ships a minimal Expo config plugin for native setup that consumers should not hand-edit. The plugin configures Android Bluetooth permissions, iOS Bluetooth usage descriptions, and ACS SDK linkage or placement needed by the native module.

