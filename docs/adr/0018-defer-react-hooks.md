# Defer React Hooks

The v0.1 package exposes plain async functions and event subscriptions, not React hooks. Hooks can be added later as small wrappers after the core Reader and MIFARE workflows stabilize in real apps.

