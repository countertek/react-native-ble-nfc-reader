# Use TypeScript Tests And Manual Hardware Checks

The v0.1 package uses TypeScript tests for public API validation, formatting, and helper behavior, plus a manual hardware checklist for Android and iOS Reader-card flows. Native automated tests are deferred because reliable BLE smart card tests require physical Readers and cards.

