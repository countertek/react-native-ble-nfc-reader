# Keep The Example App Generic

The example app demonstrates scanning, connecting, reading a card UID, authenticating with a user-entered key, reading one block, and writing one block after confirmation. It must not include hotel credential screens, bundled keys, or hotel-lock-specific data formats.

