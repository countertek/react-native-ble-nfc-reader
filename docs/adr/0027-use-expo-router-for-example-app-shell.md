# Use Expo Router For The Example App Shell

The example app uses Expo Router for its entry point and native stack shell, but keeps the manual hardware test flow on one route. This gives the example a current Expo app shape and keeps route files small without making navigation part of the Reader API example.
