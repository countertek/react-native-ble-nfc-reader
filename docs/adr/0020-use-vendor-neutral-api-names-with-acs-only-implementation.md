# Use Vendor Neutral API Names With ACS Only Implementation

The public API uses vendor-neutral Reader and MIFARE language, while v0.1 supports ACS BLE Readers only. The implementation does not introduce a reader-vendor adapter abstraction until a second vendor is actually supported.

